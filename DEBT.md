# Technical Debt Register

> The single, greppable source of truth for known debt in Hermes Station.
> Rule (`PROJECT_CONSTITUTION.md` §10): **known debt lives here**, not in
> prose comments no one can find. Each item names the location, why it's
> debt, and the exit condition that lets us delete the row.
>
> Status: `open` · `in-progress` · `done`. When `done`, keep the row for
> one release with a link to the fixing commit, then prune.
>
> Seeded from the owner review 2026-05-30.

| # | Debt | Location | Why it's debt | Exit condition | Status |
|---|---|---|---|---|---|
| **D1** | Two divergent run paths | `server/runs.py` | Different status/events for the same concept; SPA couldn't distinguish | **Done (2026-05-30 run-model pass):** *contract* unified via one `_terminal_frame()` helper (all terminals carry `session_id`) + shared status vocab + identical cleanup; executions stay distinct (streaming AIAgent vs one-shot gateway handler — inherent); slash now `await ws.broadcast`; pinned by `test_runs.py` | **done (2026-05-30)** |
| **D2** | Run-status vocabulary drift | `server/runs.py`, `src/lib/hermes-types.ts` | 3 vocabularies for one state machine | **Done (2026-05-30 run-model pass):** slash emits `failed` (was `error`); dead `RunCreated` (`pending`/`stopped`) removed; canonical `queued → running → completed/failed/cancelled` across server + WS + TS; regression test added | **done (2026-05-30)** |
| **D3** | Stale SSE types for a nonexistent endpoint | `src/lib/hermes-types.ts` | Dead doc (`GET /v1/runs/{id}/events`); everything is WS | **Done (2026-05-30 run-model pass):** `RunCreated` + the 6 SSE event interfaces removed; pointer comment to `ws-types.ts` left | **done (2026-05-30)** |
| **D4** | Pyright advisory | `.github/workflows/ci.yml` | Type safety not gated (28 known errors) | **Done (2026-05-30):** all 28 cleared (None-guards → 503 / `TypedDict` / `TypeGuard` / local narrowing); pyright is now a **hard gate** (`continue-on-error` removed) — 0 errors across 54 files | **done** |
| **D5** | Security/approval/uninstall tests not gated in CI | `.github/workflows/ci.yml` (was `--ignore`/`--deselect`) | Highest-consequence (localhost trust, approvals) untested in CI | Security/dashboard/uninstall now run in CI (via `quiet_hms_env`); upstream-only approval suites self-skip via `pytest.importorskip`; CI runs a bare `pytest tests/`. Audit also caught 2 unguarded upstream-coupled `test_discover.py` registry tests (latent CI red) and guarded them. | **done (CI hardening pass, 2026-05-30)** |
| **D6** | No ESLint for the SPA | `eslint.config.js`; CI `pnpm lint` | No hooks/dead-code/correctness lint on 27k LOC | **Done (2026-05-30):** ESLint flat config + CI gate at **`--max-warnings 0`** (0 errors, 0 warnings). Ratcheted all 21: removed dead code, typed the `any`s, `useMemo`-stabilized hook deps, justified the intentional `exhaustive-deps` + 1 cron `any` inline. Precise typing also surfaced + fixed an i18n drift (profile `startGateway`/`stopGateway` missing → zh now localized). | **done** |
| **D7** | God-components over budget | `Composer.tsx`, `FilesSideTree.tsx`, `SettingsPanel` — all split | Change-amplification | Split below ~500 LOC | **Done (2026-05-30 component-split pass):** **`SettingsPanel` split done** — 1231 → **111-line shell** + `components/settings/{SystemTab 561, SecurityTab 315, PreferencesTab 138, AppearanceTab 104, shared 24}`; tabs reallocated → Preferences / Appearance / Security / System / Advanced (Preferences = Station-global; Advanced = config.yaml editor). **Advanced is now profile-aware** (Stage 1 done): `GET/PUT /api/profiles/{name}/config` + a Profile selector edit any Profile's own `config.yaml` (each Profile is its own HERMES_HOME; default → ~/.hermes/config.yaml). sha256 optimistic-lock + YAML-validated; pinned by `test_profile_config.py`. **FORM ⇄ YAML done** (Stage 2): `ConfigForm` renders the upstream config schema (`/api/dashboard/config/schema` — scalars grouped by category + search + per-field reset; list/object defer to YAML), with values read/written per-profile via `/api/profiles/{name}/config/values` (`set_scalar_at_path`, comment-preserving; pinned by `test_profile_config.py`). Degrades to YAML when the Dashboard is down. No new private-symbol coupling (consumes the proxied dashboard endpoints, not `web_server.CONFIG_SCHEMA`). **`Composer`/`FilesSideTree` split done** (2026-05-30): `Composer.tsx` 1386 → **436** (extracted pure `lib/epub-zip.ts` + `chat/composer/{ModelPicker, ContextMeter, ReasoningPicker, parts, AttachmentChips, useComposerAttachments, useVoiceInput}`; the 4 duplicated upload paths now funnel through one `ingestFiles`; reuses the shared `useProviders.ProviderInfo` instead of a local dupe). `FilesSideTree.tsx` 904 → **252** (extracted `files/{WorkspacePicker, FileTreeNode}` — the recursive node cluster). All three named god-components now < 500 LOC; verified typecheck / lint (`--max-warnings 0`) / vitest 67 / build green. Re-audit surfaced 3 more over budget, **not** in the original D7 scope → follow-up candidates: `SessionList.tsx` (697), `ModelsPanel.tsx` (612), `SystemTab.tsx` (561, from this split — coherent lifecycle cluster, could split Diagnostics/Dashboard/Gateway). | **done (2026-05-30)** |
| **D8** | No REST contract doc | `docs/` (WS has `WS_PROTOCOL.md`; REST has none) | 21 route modules, no single source of truth | Author REST contract doc; generate/check TS types | **Done (2026-05-31):** authored [`docs/REST_API.md`](docs/REST_API.md), symmetric with `WS_PROTOCOL.md` — the full surface (~92 endpoints / 21 modules) grouped by `ARCHITECTURE.md` §2 domains, each row method+path+purpose with ⚠ marking CSRF-required mutations, plus a **cross-cutting** section documenting the middleware chain (`host_guard → cors → rate_limit → security_headers → auth → csrf`): loopback-trust auth model, `X-HMS-CSRF` rule, 100/60s rate limit, the `{"error": code}` envelope + status conventions, ID regexes. Verified by extraction: every real route covered, **zero invented** endpoints, all 35 doc links resolve. Linked from `AGENTS.md` + README. Note: "generate/check TS types" (codegen) intentionally **not** done — out of scope for the doc; left as a possible future D-row if drift becomes a problem. | **done (2026-05-31)** |
| **D9** | ~12 private upstream symbols in shim | `server/lib/upstream_shim.py` (`_load_gateway_config`, `_get_platform_tools`, `_BUILTIN_DASHBOARD_THEMES`, …) | High-churn coupling; each upstream release can break one | Per-release shim review; lobby upstream for public APIs | open (ongoing) |
| **D10** | Frontend test coverage thin (vs backend ~41%) | `tests/` | Hardest logic (chat runtime) least covered | **In progress (chat-runtime coverage pass):** chat store characterization-tested — `chat.ts` at **84% line / 68% branch** via 13 tests in `chat-reconcile.test.ts`. **2026-05-31:** extended to the rest of the chat runtime — extracted `useRunsStream`'s branch-heavy logic to pure `src/lib/run-events.ts` (`shouldApplyFrame` seq-dedup, `toolCallId`, `mapToolStatus`) + 14 tests in `run-events.test.ts`; and pinned the DB→ChatMessage rebuild (`historyToChatMessages` turn-grouping + tool back-patching + status inference) and `toolResultsById` with 8 tests in `session-messages.test.ts`. Frontend suite now **96 tests / 10 files** (was 67/7); the hardest-logic (chat-runtime) concern is now safety-netted. Remaining (lower-value, routine): other panels + non-runtime hooks (`useFiles`, `useKanban`, `useProfiles`, …). | in-progress |
| **D11** | Debt tracked in prose, not greppable | repo-wide (0 `TODO/FIXME`; "advisory for now", "audit note") | Untracked debt is unmanaged | This file exists + new debt lands here | **done (this file)** |
| **D12** | Capability gaps vs upstream (incomplete web client) | `docs/CAPABILITY_COVERAGE.md` §9 | "Complete web client" goal has measured gaps: MCP mgmt, per-platform config, personality picker, memory store/Honcho, Portal/Tool Gateway | Close ✗/◐ rows in priority order; re-audit each upstream bump | **In progress (2026-05-31):** **MCP server management ✗ → ◐** — new `server/routes/mcp.py` (`GET/POST/PATCH/DELETE /api/mcp/servers`, config-layer over `mcp_servers` in config.yaml; enable/disable is comment-preserving) + `_Mcp` shim group + `McpServersView` on `/skills` (list / toggle / remove / manual add for stdio+http, i18n en+zh). Catalog git-install + OAuth deliberately stay in the CLI (security/coupling). Pinned by `test_mcp.py` (8). Docs updated (REST_API, CAPABILITY_COVERAGE). Remaining ✗/◐: per-platform config, personality picker, memory store/Honcho, Portal/Tool Gateway. | in-progress |
| **D13** | E2E harness still unbootstrapped | `tests/e2e/README.md` | Playwright coverage is only a tracked placeholder; browser-only flows still lack executable smoke tests | Link the owning issue, add `playwright.config.ts`, install Playwright, and land at least one browser smoke test for the main path | **Done (2026-05-31):** installed `@playwright/test` + Chromium; `playwright.config.ts` at root (webServer builds + previews `dist/` on :4173; `testMatch` = `*.spec.ts`). 4 smoke tests in [`tests/e2e/smoke.spec.ts`](tests/e2e/smoke.spec.ts) covering the main path — loopback boot → Sessions shell, module-tab navigation (D19 end-to-end), degraded-mode gate, login flow — all `/api/*` mocked via route interception ([`mocks.ts`](tests/e2e/mocks.ts)) so **no Python/hermes-agent needed** (clean-CI runnable like Vitest). Wired as a separate **`E2E (Playwright)`** CI job; Vitest config excludes `tests/e2e/**` so the runners don't collide; `pnpm e2e` script; artifacts gitignored; README flipped to active. typecheck / lint / vitest 96 / build all green; `pnpm e2e` → 4 passed. | **done (2026-05-31)** |
| **D14** | God-components surfaced by the post-D7 re-audit | `src/components/chat/SessionRecents.tsx` (697, was `SessionList`), `src/panels/ModelsPanel.tsx` (612), `src/components/settings/SystemTab.tsx` (561) | Same change-amplification as D7, but **outside** D7's named scope — surfaced 2026-05-30 after `Composer`/`FilesSideTree` landed. `SessionRecents` = the Recents rail (shared by `SidebarRecents`), **not** the `/sessions` panel; `SystemTab` is a product of the SettingsPanel split (coherent Diagnostics/Dashboard/Gateway/lifecycle cluster). | Split below ~500 LOC. `SessionRecents` is logic-dense (shares chat-store `setActiveSession`/recents paths) → add characterization tests before splitting, like the `Composer` precedent. | open |
| **D15** | `/sessions` eagerly bundles the full message-render stack + duplicates `/chat`'s thread container | `src/panels/SessionsPanel.tsx` (top-level `import MessageBubble` → `react-markdown` + `remark-gfm` + `highlight.js`); `src/components/chat/MessageList.tsx` | Panels are `React.lazy`, but SessionsPanel statically imports `MessageBubble`, so **opening the `/sessions` table pulls markdown + highlight.js before any preview is opened**. Separately, MessageList (live: chat store / streaming / auto-scroll) and the SessionsPanel drawer (static: `historyToChatMessages` → `MessageBubble.map`) each hand-roll a "messages → bubbles + empty/loading/error" container — the bubble is shared, the container is duplicated. | (1) lazy-load the preview renderer inside the drawer (`React.lazy`) so the table renders without the heavy stack; (2) extract a presentational `<MessageThread messages={ChatMessage[]}>` reused by MessageList (with live concerns layered on) + the SessionsPanel preview; (3) add an "Open in /chat" button to the preview drawer header (`setActiveSession(id)` + `navigate('/chat')`, 1:1 with the Recents pick path). | **Done (2026-05-31):** extracted presentational [`MessageThread`](src/components/chat/MessageThread.tsx) — it `lazy()`-loads `MessageBubble`, so the heavy stack is code-split into its own chunk. `MessageList` layers live concerns on top (`scrollRef`/`onScroll`/`footer` sentinel); `SessionsPanel`'s drawer renders `MessageThread` directly + gained an **Open in chat** button (`setActiveSession` + `navigate('/chat')`). Verified by build: `MessageBubble` is now a standalone **987 kB** chunk; the `SessionsPanel` chunk is **10 kB** and markdown-free — opening the `/sessions` table no longer pulls markdown/highlight.js until a bubble renders. typecheck / lint / vitest 67 / build green. | **done (2026-05-31)** |
| **D16** | README not aligned to the governance identity + lacks the standard section structure | `README.md` | Intro reads "gateway platform plugin / exposes the same … surface" instead of the canonical **"complete web client"** wording fixed in `PROJECT_CONSTITUTION.md` (§ identity) and `ARCHITECTURE.md`; sections are ad-hoc vs the upstream README style (badge block + Quick Start / Contributing / License). | Restructure to **Introduction / What's inside / Screenshots / Quick Start / Support / Contributing / License** (upstream `hermes-agent` README style + shields.io badges), open with the canonical "complete web client … in-process platform plugin" identity, and keep the existing **Security-boundary** warning (own section or folded into Quick Start). Also document the `session` (persisted record, upstream term) vs `chat` (live session view) naming convention in `ARCHITECTURE.md` to prevent the `SessionList`↔`/sessions` confusion. | **README restructured 2026-05-31** (upstream-style emoji sections + shields.io badges + canonical "complete web client / in-process platform plugin" identity + Support/ETH + preserved Security boundary; no `README.zh-CN.md` by request). The `session` (persisted, upstream term) vs `chat` (live session view) naming convention is now documented in `ARCHITECTURE.md` §2, clarifying that `SessionList` powers the Recents rail (not `/sessions`). | **done (2026-05-31)** |
| **D17** | Composer profile pill doesn't re-scope the active chat | `src/components/chat/Composer.tsx` (pill → `useSetActiveProfile`), `src/hooks/useProfiles.ts` | The quick-switch pill POSTs `/api/profiles/active` (sets the **sticky** default) but the run body never carries a profile, and Station runs in-process under one gateway/`HERMES_HOME`, so picking a profile doesn't change which agent the next message hits. The API even returns `current` vs `sticky` + `requires_restart`, but the pill only reads `.sticky`. **Decision (owner): keep the pill** (it's the fast switcher; the Profile panel already supports multiple profiles active at once) and **don't** add restart-hint labels — fix the underlying behaviour so the selected profile actually takes effect. Root cause to be pinned at implementation. | The pill's selection demonstrably re-scopes subsequent runs to the chosen profile (or the mechanism by which multiple concurrent gateways are addressed is wired through the run path), with a regression test. | **Done (2026-05-31):** in-process re-scope, no restart — same mechanism upstream's cron scheduler uses for per-job profiles (researched `hermes-webui` "seamless switching, no restart" → it relies on upstream's `set_hermes_home_override` `ContextVar`). New `server/lib/profile_run.py` `profile_home_override(name)` (mirrors `cron/scheduler._job_profile_context`: sets the override + `os.environ` delta snapshot/restore); `state_db.db_for_home()` binds the run to that profile's own `state.db` (the override can't reach an already-built SessionDB); `runs.start_run(profile=)` threads it (`RunHandle.profile`), wrapping `_build_agent` + `run_conversation` on the worker thread (ContextVar set there since `run_in_executor` doesn't copy context). Route validates `profile` (`_PROFILE_ID_RE`); SPA `useRunsStream` sends the sticky active profile; shim exposes `set/reset_hermes_home_override`; `state_db_path(home)` keeps the no-hardcoding gate clean. Pinned by `test_profile_run.py` (9) + `test_chat_flow.py` (+2). Verified: CI-sim 342p/11s, agent-venv 368p, ruff / pyright / no-hardcoding / pnpm typecheck·lint·vitest 67·build all green. | **done (2026-05-31)** |
| **D18** | Chat workspace / cwd awareness gaps | `server/runs.py` (`start_slash_run` 244–311 has no workspace preface; agent path injects one at 344), `src/panels/ChatPanel.tsx` / `src/components/chat/Composer.tsx` (no cwd indicator) | Workspace→agent plumbing exists (`server/lib/workspace_cwd.py` sets `TERMINAL_CWD` on boot + on workspace switch; `runs.py` injects a per-run `_workspace_context_history` preface), but: (1) the **slash-run path has no workspace preface**; (2) the chat UI shows **no indicator** of the agent's active workspace/cwd; (3) the file-tree **root** (hermes vs workspace) is conflated with the **active workspace**, so the panel view can diverge from the agent's cwd. | Inject the workspace preface into the slash path too; surface "working in: `<name>`" in the chat header/composer (from `active_workspace()`); document/clarify root-vs-cwd. | **Done (2026-05-31):** **(1) corrected, not done as written** — the slash path dispatches *deterministic* gateway commands (`/new`, `/compress`, `/help`, …) to upstream's `_handle_message`, **not** an agent file-ops turn; a `MessageEvent` has no `conversation_history` slot and prefixing the workspace hint to the command text would corrupt it (`/help` → garbage). Tools already see the dir via process-global `TERMINAL_CWD`, so the slash path needs no preface — documented as intentional in ARCHITECTURE §2. **(2) cwd indicator** — new `GET /api/files/workspace/active` → `{name, cwd}` (the agent's *resolved* cwd via `workspace_cwd.resolve_active_cwd`, not just an explicit selection); `useActiveWorkspaceCwd` hook (invalidated on workspace switch); `ChatTitleBar` shows a "working in: `<name>`" chip in the `PageTopBar` subtitle (FolderGit2 icon, full path on hover); i18n `nav.workingIn` (en/zh/types). **(3) root-vs-cwd** documented in `ARCHITECTURE.md` §2 (browsing root ≠ agent cwd; only the active workspace seeds `TERMINAL_CWD` + the per-run preface) — and fixed the stale `SessionList`/`MessageList` names in that section post-D21. Pinned by `test_files_route.py` (+2). Verified: CI-sim 344p/11s, agent-venv 370p, ruff / pyright / no-hardcoding / typecheck / lint / vitest 67 / build all green. | **done (2026-05-31)** |
| **D19** | Sidebar module switch doesn't navigate | `src/components/layout/Sidebar.tsx` (`setModule`) | Clicking a module tab (`agent`/`tasks`/`manage`) only sets state + localStorage — it does **not** navigate, so the page stays put while the nav list changes underneath. The reverse (route → module auto-follow) already works via a `useEffect`. Asymmetric and confusing. | On `setModule(m)`, navigate to that module's first/default route (`ROUTES.filter(module===m).sort(order)[0]`) unless the current route already belongs to `m`. | **Done (2026-05-31):** new `handleModuleClick(m)` on both module-tab buttons (collapsed + expanded) sets the module **and** navigates. Decision logic extracted to pure `registry.ts` helpers — `moduleForPath`, `firstRouteForModule`, `moduleNavTarget(m, currentPath)` (returns `null` to stay put when already in the module, so a hidden in-module route like `/chat` or `/files` isn't yanked to its sibling). Pinned by `tests/unit/routes/module-nav.test.ts` (7). typecheck / lint / vitest 74 / build green. | **done (2026-05-31)** |
| **D20** | No UI-conventions contract; uneven primitive adoption | repo-wide (`src/components/ui/*` exist but only ~15 files use `Button`, 4 use `IconButton`; **8 panels hand-roll `<button>`**; **~15 files** hand-roll `onMouseEnter` hover instead of CSS `--hms-hover-bg`) | Tokens (`--hms-hover-bg`, `--hms-radius-*`, `--hms-space-*`, styled scrollbar) and primitives exist but there's no written rule for *when* to use which primitive, hover rules, two-column list-row style, or input/select/search styling — so details drift (button sizes, hover bg, which elements get hover). | Author a UI contract (`docs/UI_CONVENTIONS.md` or ARCHITECTURE §) covering buttons/icon-buttons, hover (CSS-only), list rows, inputs/selects/search, scrollbars; migrate hand-rolled buttons/hover to primitives opportunistically. | **Done (2026-05-31):** authored [`docs/UI_CONVENTIONS.md`](docs/UI_CONVENTIONS.md) — the single source of truth for UI detail: buttons (`<Button>` sizes sm/md/lg + variants; `<IconButton>`; no fourth height), **hover = CSS not JS** (`hms-sidebar-row`/`hms-btn` + `--hms-hover-bg`/`--hms-selected-bg`; JS hover only for sibling/portal state), list rows + `<PanelTwoColumn>`, inputs/selects/search (`<SearchInput>`/`<Field>`/`<PopupSelect>` + `--hms-input-*`), globally-styled scrollbars, the spacing/radius/type/color token scales, and icons. Names the real migration surface (8 hand-rolled-`<button>` panels, ~15 inline-hover files) as an opportunistic worklist with the legitimately-exempt cases called out. Proved the contract with 2 migrations: new `.hms-card-hoverable` class → `AnalyticsPanel` StatCard (inline border-hover removed); `SessionsPanel` table rows → `hms-sidebar-row` + `data-active` (inline hover + bg removed). Linked from `AGENTS.md` + README. typecheck / lint / vitest 74 / build green; inline-hover offenders 15 → 13. | **done (2026-05-31)** |
| **D21** | `chat/` filenames don't read as the live-chat view | `src/components/chat/{ChatStream,ChatBubble,ChatThread,SessionRecents}.tsx` (were `MessageList`/`MessageBubble`/`MessageThread`/`SessionList`) | Per the `session` vs `chat` convention (ARCHITECTURE §2), the live-view files should read as "chat …". Renamed `MessageList → ChatStream`, `MessageBubble → ChatBubble`, `MessageThread → ChatThread`; and `SessionList → SessionRecents` (it lists *sessions* for the Recents rail — **not** `ChatRecents`, which would break the convention). | All four renamed (file + internal identifiers + importers + doc refs); typecheck / lint / vitest / build green. | **done (2026-05-31):** `git mv` (history-preserving) all four + internal IDs (`MessageBubbleInner→ChatBubbleInner`, `MessageThreadLabels→ChatThreadLabels`, `SessionListProps→SessionRecentsProps`); updated importers (ChatPanel, SidebarRecents, SessionsPanel) + the lazy `import("./ChatBubble")` + all doc refs. Zero stale refs; clean build re-emits the lazy chunk as `ChatBubble-*.js` (D15 split intact). typecheck / lint (`--max-warnings 0`) / vitest 67 / build all green. |
| **D22** | PageTopBar abandoned the equal-height header design | `src/components/layout/PageTopBar.tsx` | The bar was originally designed to keep the header strictly equal-height across panels; it now moves the border below an optional `context` row (filters/tabs), so pages with a second row look taller. Currently *consistent within the protocol*, but diverges from the intended equal-height design. **Decision (owner): track as a task; revisit the design details later.** | Decide + implement the final header-height contract (equal-height vs context-row border), apply uniformly. | **Done (2026-05-31):** (A1) `PageTopBar` title row is now **fixed `--hms-header-h` + always-bordered**, so every page's title bar lines up at the same height; a `context` row is a distinct band below (not folded into the header border). (B1) migrated the hand-rolled header buttons to D20 primitives — `ChannelsPanel`/`GroupPanel`/`CronPanel` raw `<button>` + `ChatTitleBar`'s `iconBtnStyle()` → `<Button>`/`<IconButton>` (3 local style consts deleted). (B2) moved *view-control selectors* out of `actions` into `context` (Kanban board picker, Analytics time-range) so `actions` = page-level actions only. Documented the header protocol in `docs/UI_CONVENTIONS.md` §8 (actions = actions, context = view controls). typecheck / lint / vitest 96 / build / e2e 4 green. | **done (2026-05-31)** |
| **D23** | Approval "session" / "always" still re-prompts | `server/approvals.py`; upstream `tools/approval.py` | Owner-reported: after "Approve for session" / "Approve permanently", the same command re-prompts on a later request. **Code path read clean:** Station only forwards `choice` to upstream `resolve_gateway_approval`; persistence (`approve_session` / `command_allowlist`) + the skip-check (`is_approved`) both key off the **same** `session_key` (= `handle.session_id`, consistent across `register`/`bind_session_key`), and upstream's `tool_executor` uses `propagate_context_to_thread` to copy `_approval_session_key` into tool worker threads — so the mechanism *should* hold. **Needs runtime reproduction** on a live gateway + hermes-agent to localize (could be a `session_key` boundary, a per-run vs per-session scope mismatch, or an upstream bug). Don't change code until reproduced. | Reproduce live; identify whether the persisted approval is keyed/checked under a different `session_key` (e.g. across runs) or whether it's an upstream defect; fix or file upstream. | open (needs live repro) |
| **D24** | Profile runs persist sessions to the **default** profile's `state.db` | `server/runs.py` `_build_agent` (`session_db=db()`) | Owner-reported + **root-caused**: the D17 profile re-scope points config/.env/skills at the chosen profile (via `profile_home_override`) and fixes history *loading* (`db_for_home`), but `_build_agent` always constructs the agent with `session_db=db()` — the **default-home** SessionDB. Upstream persists to the SessionDB it's *given* ("frontends pass `session_db` explicitly"), so a profile run is read-correct but **writes its session rows into `~/.hermes/state.db`**. The agent "recognizes" the profile yet its data lands in default — exactly the report. This is the D17 fix's missing half. | In `_build_agent`, pass `db_for_home(profile_home)` (a named profile's own `state.db`) instead of `db()` when a profile is active; pin with a test asserting a named-profile run reads/writes that profile's DB. | **Done (2026-05-31):** `_build_agent` now resolves `profile_home = resolve_profile_home(handle.profile)` and passes `session_db=db() if profile_home is None else db_for_home(profile_home)` — a named profile reads *and* writes its own `state.db`, the default profile keeps the shared singleton. Also threaded `profile` into `_maybe_auto_title` (the auto-title write was the same default-DB leak) so the generated title lands in the same profile DB as the session. Pinned by `test_runs.py` (+3: named→profile DB, default→default DB, auto-title→profile DB). CI-faithful (py311) pyright 0 / pytest 360p+11s / ruff / typecheck / lint / vitest 96 / build green. | **done (2026-05-31)** |
| **D25** | Workspace switching: ~/.hermes didn't set agent cwd; D18 chip; "Default" label | `src/components/files/{FilesSideTree,WorkspacePicker}.tsx`, `server/routes/files.py`, `src/components/chat/ChatTitleBar.tsx` | Owner-reported trio: (a) selecting **~/.hermes** in the workspace switcher only changed the file-tree browse root, leaving the agent's `TERMINAL_CWD` on the prior workspace (the root-vs-cwd split was *too* strict); (b) the D18 "working in" chip in the chat header was unwanted; (c) the dropdown label read "Default — ~/workspace". | **Done (2026-05-31):** (a) added a **`"hermes"` active-workspace sentinel** (`files.py HERMES_ACTIVE_ID`) — `active_workspace()` resolves it to `$HERMES_HOME`, `set_active_workspace` accepts it, and `FilesSideTree.onSelectHermes` now sets it (so ~/.hermes drives `TERMINAL_CWD` like any workspace), with the picker's check-mark reflecting `activeId==="hermes"`; pinned by `test_files_route.py` (+2). (b) removed the chip + its now-dead frontend display layer (`useActiveWorkspaceCwd` hook, `ActiveWorkspace` type, `nav.workingIn` i18n, `FolderGit2`/`workingLabel`); the `GET /api/files/workspace/active` endpoint stays (clean read-model, still tested). (c) label → just `~/workspace`. Updated the ARCHITECTURE §2 root-vs-cwd note to match. **Follow-up (same day):** (d) `~/workspace` (the default) resolved to the *process* cwd (in dev = the repo) because the per-run preface was only injected for an explicit workspace — `resolve_active_cwd` now **auto-creates `~/workspace`** if missing and `_workspace_context_history` uses it for *every* run (default included), so the agent always gets an absolute working dir; pinned by `test_workspace_cwd.py` (3). (e) refresh didn't restore the selection (the `useFilesSelection` store isn't persisted) — `FilesSideTree` now syncs its browse root from the server-persisted `active_id` (`"hermes"`→hermes, else workspace) on first load. Verified: CI-sim 349p, agent-venv 44p, typecheck / lint / vitest 96 / build / no-hardcoding green. | **done (2026-05-31)** |
| **D26** | Pyright CI red on release — invisible to a mis-resolved local pyright | `server/{app.py,app_keys.py,routes/upload.py}`; `.github/workflows/ci.yml` | The first release CI's Python job failed with **8 aiohttp-typed errors** (`FileResponse` not assignable to `Response` ×2; `AppKey("…", Any)` → `type[Any]` ×2; `MultipartReader` missing `.read`/`.filename`/`.read_chunk` ×4). **Invisible locally** because a bare `pyright server/` resolved aiohttp from the wrong interpreter (types went permissive); only `pyright --pythonpath <venv>` — what CI effectively does — surfaces them (earlier mis-diagnosed as a "flake"). | **Done (2026-05-31):** `_serve_index`/`handle_download` now return `web.StreamResponse` (the FileResponse/Response base); dropped the invalid `AppKey("…", Any)` 2nd arg (the LHS generic already types the key); narrowed multipart parts to `BodyPartReader` (skip nested groups). Confirmed CI-faithfully (`pyright --pythonpath <venv> server/` → 0 errors). Local habit: run pyright against the project venv. | **done (2026-05-31)** |
| **D27** | Real-time chat content lost on refresh / session-switch / new-session | `src/store/chat.ts`, `src/hooks/useRunsStream.ts`, `server/runs.py` (WS stream) | Owner-reported: during a live turn, refreshing, switching sessions back-and-forth, or clicking "new session" drops the in-flight content. Root cause: **streaming deltas are WS-ephemeral** — durable in `state.db` only once the run *completes*; the store persists `messages` + `runningBySession` (re-attach) but the WS stream has **no replay/back-fill of frames missed during the disconnect**, and `setActiveSession` clears `messages[]` then rebuilds from DB (which lacks the unfinished turn). **Priority: P1.** | Make in-flight turns durable/resumable: (a) server buffers the run's accumulated output + a `GET /api/runs/{id}/transcript?since=<seq>` catch-up the re-attach replays (seq machinery already in `run-events.ts`), or (b) persist partial assistant text incrementally so `reconcileSession` rebuilds it. **Design approved (owner, 2026-06-01) — Buffer + Replay + Snapshot (approach A):** (1) **Buffer** — each `RunHandle` keeps an in-memory ordered list of emitted frames (delta / tool / reasoning) keyed by the existing `seq`, dropped on `run.completed`. (2) **Replay** — `GET /api/runs/{id}/transcript?since=<seq>` returns buffered frames > seq; on re-attach (refresh / switch-back / new-then-back) the client replays them, de-duped by `shouldApplyFrame`. (3) **Snapshot (crash safety)** — async-flush the buffer to a per-run sidecar (or a `partial` column) every N frames / T ms so a gateway crash recovers the partial turn, rendered as *interrupted* (not resumed — the AIAgent worker thread is gone). Completed turns keep landing in `state.db`; the snapshot only covers the in-flight window. Pin: switch-away-mid-run store test + a replay-endpoint test. **Phase 1 done (2026-06-01) — Buffer + Replay:** `RunHandle` now keeps a **durable accumulator** (`partial_text`/`partial_reasoning`/`partial_tools`, folded in `stamp()`) that — unlike the bounded 512-frame ring — grows with the *response*, so a long run that overflowed the ring still rebuilds. New `GET /api/runs/{id}/transcript[?since=]` exposes it; `useRunsStream.attachRun` **seeds the in-flight bubble from the transcript before subscribing**, then the WS ring-replay continues (deduped by `lastSeqRef`/`shouldApplyFrame`), so refresh / session-switch / new-then-back reconstruct the full partial; also set the previously-unset `offRunEventRef` (clean detach). Pinned by `test_runs.py` (+4: accumulator, ring-eviction survival, transcript 200/404). typecheck/lint/vitest 96/build/e2e 4/pyright 0/pytest 370 green. **Needs runtime verification with a live stream.** **Phase 2 done (2026-06-01) — Snapshot/crash:** new `server/lib/run_snapshot.py` checkpoints the in-flight partial to a per-run sidecar (`hms_data_dir()/run-snapshots/`) every 3s while running (`_snapshot_loop`), deletes it on any terminal, and supersedes stale ones when a new turn starts; a sidecar that outlives its run = a crash, surfaced by `GET /api/sessions/{id}/interrupted` (null when the run is still live in-registry) and rendered by `ChatPanel` as a trailing *interrupted* assistant bubble on load; startup sweeps sidecars >7d. Pinned by `test_run_snapshot.py` (9). pyright 0 / pytest 379 / typecheck / lint / vitest 96 / build / e2e 4 / hardcoding green. **Both phases need runtime verification with a live stream + an induced crash.** **Follow-up (2026-06-01) — user-message recovery:** owner re-test showed the *user* bubble still vanished on a mid-run refresh (upstream persists it to `state.db` only on completion, and the accumulator held only the assistant side). Fixed: `RunHandle.user_input` (the prompt) now rides the transcript + snapshot; `attachRun` restores the user bubble before seeding the assistant partial, and the crash-recovery bubble shows it too. Pinned by `test_runs.py`/`test_run_snapshot.py` (+3). **Known minor edge (separate follow-up):** a *brand-new* in-flight session isn't listed in `SessionRecents` until completion (the optimistic row doesn't survive a refresh, and `SessionRecents` deliberately renders no placeholder rows) — the chat itself recovers; only the sidebar row is briefly absent. **Follow-up 2 (2026-06-01) — existing-session loss:** the user bubble still vanished in an *existing* session because `reconcileSession` dropped the live user whenever the DB rebuild had *any* user (always true once there are prior turns), which also dropped the re-attach seed. Now it dedups by content against the rebuild's *last* user, so the in-flight prompt survives a refresh/switch in existing sessions too. Pinned by `chat-reconcile.test.ts` (split into keep-prior-turn + dedup-same-content). | done (2026-06-01) |
| **D28** | Pages can't show a *chosen* profile's data (skills + which pages) | `src/hooks/useSkills.ts`, `server/routes/skills_content.py`; cross-cutting | Owner-reported + design Q: the Composer profile pill (D17) re-scopes a *run*, but read-only pages still read the **process/default HERMES_HOME**, so they can't display another profile's skills/data. Backend already supports per-profile reads (`/api/profiles/{name}/config`, `db_for_home`, `resolve_profile_home`); missing is a profile param on the skills surface + a **global "view profile" scope**. **Audit — profile-scoped pages needing it:** Skills/Toolsets/MCP, Sessions (D29/D30), Models, Cron, Files. **Gateway-global (not scoped):** Logs, Analytics, Settings→System. **Priority: P2; needs an owner call on the scope model (per-page selector vs one global view-profile).** | **Design approved (owner, 2026-06-01) — global Profile Scope + per-page Override:** a client-side **view-profile scope** (default = active profile) threads `?profile=<scope>` to profile-aware reads (`db_for_home`/`resolve_profile_home` already support it); a page may locally **override** it (advanced). **Critical invariant:** the view scope is **read-only** and *decoupled* from the Composer profile pill (the run/**write** scope, D17) — viewing profile X ≠ running under X — so view-scoping can never cause cross-profile writes. Surface both indicators clearly so "what am I viewing vs running" is never ambiguous. Rollout: add the scope store + selector, thread `?profile=` per profile-aware route (skills/toolsets/mcp/models/cron/files/sessions), backend reads under that home. **Profile gap G4 (2026-06-02):** the Profile panel is the natural entry point for this — from a selected (non-active) profile, manage its **skills / cron / channels / MCP / model** either via the profile-scope override above, or via deep-links from Profile pre-scoped to that profile (`?profile=<name>`). Same scope model; D52 (export/import) + D44 (memory) + D51 (personality) already follow this per-profile-from-Profile pattern. | open (P2, design-ready) |
| **D29** | Non-default-profile chat: SessionRecents hides that profile's sessions (+ likely more) | `src/components/chat/SessionRecents.tsx`, `server/routes/chat.py` (`/api/sessions`) | Owner-reported: switching chat to a non-default profile has multiple rough edges; the **known** one — `SessionRecents` (and `/sessions`) list only the **default home's `state.db`**, so a profile's own sessions never appear. Same root as D24/D30 (per-home `state.db`, listing not profile-aware). Other non-default-profile chat bugs suspected but unconfirmed. **Priority: P2 (pairs with D30).** | `/api/sessions` aggregates across profile homes (or `?profile=`), tagging each row's profile; SessionRecents surfaces the selected profile's sessions; enumerate + fix the other suspected non-default chat bugs. **Done (2026-06-02):** `GET /api/sessions` now reads **every profile's `state.db`** (`_profile_homes()` = default + named via `shim.profiles.list_profiles` → `db_for_home`), merges + sorts + pages, and tags each row with its `profile`; a default-DB error stays 500, a named profile's error is skipped. So non-default-profile chats appear in Recents + `/sessions`. Pinned by `test_sessions_list.py` (3). **Needs runtime verification** with real multi-profile state.dbs (reads N DBs per call — fine for a few profiles). Other suspected non-default chat bugs: enumerate as found. | done (2026-06-02) |
| **D30** | `/sessions` has no profile column | `src/panels/SessionsPanel.tsx`, `src/lib/hermes-types.ts` (`SessionSummary`), `server/routes/chat.py` | Owner request: add a **profile** field to the `/sessions` table, just right of `SOURCE`. Blocked on the same backend gap as D29 — sessions are read from one home and carry no profile tag. **Priority: P2 (do with D29 — one backend change).** | `/api/sessions` returns a `profile` per session; `SessionSummary` gains `profile`; SessionsPanel renders a column after SOURCE (+ optional filter). **Done (2026-06-02):** `/api/sessions` rows carry `profile` (D29); `SessionSummary` gained `profile`; `SessionsPanel` renders a **Profile column right of Source** (the default profile shows as `default`). | done (2026-06-02) |
| **D31** | dev/prod relaunch runs under the wrong HERMES_HOME for a non-default profile | `server/cli.py` (`_spawn`), `server/lifecycle.py` (`spawn_hermes_gateway_restart`) | Owner-reported (`hms dev` and `hms restart` under active profile 'creative'): upstream warns *"HERMES_HOME is unset but active profile is 'creative'. Falling back to ~/.hermes … data this process writes will land in the wrong profile."* The dev spawner did `env = os.environ.copy()` without HERMES_HOME; restart ran plain `hermes gateway restart` (no `-p`), unlike `spawn_profile_gateway`. Same class as D24 (wrong-profile writes). | **Done (2026-05-31):** added `profile_run.active_profile_name()` / `active_profile_home()` (sticky `~/.hermes/active_profile`; `None` for default). `_spawn` (dev) sets `env["HERMES_HOME"]` to the active profile's home (dev socket stays pinned via `HMS_DEV_SOCK`, so Vite still proxies); `spawn_hermes_gateway_restart` (prod) runs `hermes -p <active> gateway restart` + drops inherited `HERMES_HOME`. Default → unchanged. Pinned by `test_profile_run.py` (+4), `test_lifecycle_routes.py` (+2). **CORRECTION (owner re-test 2026-06-01): `hms restart` STILL warns.** The fix landed on the **wrong path**: `spawn_hermes_gateway_restart` is only the **REST endpoint** (SPA button, detached). The **CLI `hms restart`** goes `_cmd_restart → restart_gateway() → upstream launchd_restart()/systemd_restart()` (the log's "✓ Service restarted"), which restarts the **installed service under its plist/unit env** — that env, baked at `hermes gateway install`, carries no `HERMES_HOME` for a profile switched *after* install. The dev (`_spawn`) + REST (spawn) fixes stand; the CLI/service path does not. **Real fix (needs runtime test, partly upstream #18594):** either route `hms restart` through `hermes -p <active> gateway restart` (if upstream re-applies the home on kickstart) **or** bake `HERMES_HOME=<profile home>` into the plist/unit at install + re-install on profile switch; if neither, detect plist≠active-profile and warn. **Done (2026-06-01) — CLI routed through `-p`:** `_cmd_restart` resolves the active profile and, when non-default, calls new `lifecycle.restart_gateway_under_profile()` → `hermes -p <active> gateway restart` synchronously (streams upstream's output), delegating home resolution to upstream's CLI (the same `-p` path start/stop use) instead of the in-process `restart_gateway()` that kickstarts the install-time plist. Default profile unchanged. Pinned by `test_lifecycle_routes.py` (+2). **Runtime-verify:** confirm `hms restart` under a non-default profile no longer warns + comes up on the right home; if upstream's `-p gateway restart` merely kickstarts the existing plist (doesn't re-apply HERMES_HOME), escalate to plist re-install / upstream #18594. **FINAL (owner re-test 2026-06-02): the `-p` CLI route BROKE `hms restart`** — upstream refuses with *"Refusing to restart the gateway from inside the gateway process … blocked to prevent restart loops"* (its loop-guard fires for `hermes -p … gateway restart` run synchronously from the CLI). **Reverted** `_cmd_restart` to the in-process `restart_gateway()` (works again) + removed `restart_gateway_under_profile` and its tests. **Conclusion:** the dev (`_spawn`) home fix stands; the relaunched *service*'s HERMES_HOME is the launchd/systemd **plist env** (baked at `gateway install`), which a restart can't change and which Station can't rewrite at restart time without tripping the loop-guard or re-installing the service — this is **upstream #18594**. Workaround: re-install the service for the active profile (`hermes -p <profile> gateway install`) after switching. **Approaches (owner to pick; suspended):** (1) ⭐ **per-profile gateway services** (upstream's multi-gateway model; Station already has per-profile start/stop UI) — stop the default gateway, start the creative one (`hermes -p creative gateway start`, each with its own plist/home); cleanest, sidesteps "one gateway switching profiles". (2) **detect + warn**: `hms restart` warns when active-profile ≠ the service's home and points to `hermes -p creative gateway install` (safe, no auto-fix). (3) **`hms switch-profile <name>`** = install + restart atomically (re-bakes the plist). (4) **upstream #18594**: plist bakes HERMES_HOME, or the gateway reads the sticky `active_profile` at startup (the proper fix, upstream's). **Data note:** a wrong-home restart does **not** delete the profile's on-disk data; chat runs stay correctly scoped (D17/D24), but gateway-level ops (cron/kanban) run on default and new non-chat writes land in default (→ D37 #3). | blocked (upstream #18594; dev path done; approaches suspended) |
| **D32** | `/settings` → System shows no Hermes Station version | `server/lib/upstream_shim.py` (`CapabilityFlags`), `server/app.py`, `src/components/settings/SystemTab.tsx`, `src/store/capabilities.ts` | Owner request: surface the running Station version (System already showed hermes-agent + Python). | **Done (2026-05-31):** `CapabilityFlags.station_version` from `importlib.metadata.version("hermes-station")` in `probe()` (auto-serialized via `to_dict`), typed on the frontend flags, rendered **"Hermes Station: `<v>`"** first in the System version block. Pinned by `test_upstream_shim.py` (+2). | **done (2026-05-31)** |
| **D33** | Station doesn't convey the chat channel to hermes (flat platform identity) | `server/runs.py` (`set_session_vars(platform="station", …)`), `server/lib/upstream_shim.py` (`get_platform_tools`) | Owner-reported + research ask: Station tags every run with a flat `platform="station"` + `chat_id=session_id`, so the agent can't perceive *which* channel/surface it's on. Upstream's built-in gateways (telegram, discord, …) register as first-class platforms and pass richer channel context (chat type / title / id) that gates platform-aware tools, notifications, and the runtime footer. Need to (1) confirm whether "station" is a recognized upstream platform or should register as one, and (2) decide what channel metadata Station should pass so platform-aware behavior is correct for a web surface. | Research upstream's platform registry + the `set_session_vars` / `get_platform_tools` contract (compare telegram); register "station" properly and/or pass session-as-channel metadata so the agent + platform-aware tools behave correctly. | open (P3 — research) |
| **D34** | Web client misses upstream's auxiliary tool cards / notifications | `src/lib/run-events.ts`, `src/hooks/useRunsStream.ts`, `server/runs.py` (agent callbacks) | Owner-reported (render on telegram, not Station): ⏳ *"Working — 9 min — receiving stream response"* (long-run progress heartbeat), 💾 *"Self-improvement review: Skill 'X' created"* (skill-creation notice), ⚠️ *"Gateway shutting down — task will be interrupted"* (lifecycle warning). Station's run-event → card mapping covers `message.delta` / `tool.started` / `tool.completed` / `reasoning.available` only; upstream's **auxiliary notification surface** (progress heartbeat, self-improvement/skill review, gateway-shutdown, the `_emit_auxiliary_failure` / runtime-footer channels) isn't wired into Station's WS stream or rendered. | Enumerate upstream's auxiliary notification callbacks/events, forward them over the run WS, and render each as a Station notice/tool card (i18n en+zh). **Root-caused (2026-06-02):** these are upstream **platform messages** the gateway pushes via `adapter.send(chat_id, content, …)` (telegram renders them inline) — but `StationAdapter.send()` is a **no-op** (`"send() is unused"`), so Station drops them all (progress status, self-improvement, cron auto-deliver, gateway-shutdown). **Plan:** implement `send()` to forward as a `platform.notice` WS frame on the session's active-run channel, rendered as a distinct system-notice card; needs care for (a) the worker-thread→loop boundary (`broadcast_threadsafe`), (b) **not** duplicating the interactive response (which streams via the AIAgent callbacks, not `send()`), (c) idle / no-active-run messages (would need a session-scoped channel). **Needs runtime verification of the gateway→`send()` flow** (not exercisable in CI) → not shipped blind. **Gateway-shutdown card: owner decision pending** (telegram push-status vs a Station use). | open (P2 — root-caused; plan pending owner confirm) |
| **D35** | New in-flight session missing from Recents until completion | `src/components/chat/SessionRecents.tsx`; `server/routes/chat.py` / `runs.py` | A brand-new session isn't in `state.db` until its run completes, and the optimistic sidebar row (`useRunsStream.sendMessage`) doesn't survive a refresh — so a refresh right after the first send drops the Recents row (the chat itself recovers via D27). `SessionRecents` *deliberately* renders no placeholder rows (documented), so this needs an owner call. **Options:** (1) client-only synthetic row from the active session/run — fast, but breaks the no-placeholder rule + needs title/timestamp synthesis; (2) persist the react-query sessions cache to localStorage — optimistic row survives, but broad + staleness; (3) `/api/sessions` also lists active-run sessions not yet in the DB — server source of truth; (4) write the session row to `state.db` at run *start* — cleanest conceptually, but touches upstream's DB + crash cleanup; (5) **[recommended]** small `GET /api/runs/active` (run_id / session_id / started_at / first-user-text from the in-memory registry) that `SessionRecents` merges as an "in progress" row — Station-owned, survives refresh, no upstream DB write. | Owner picks an approach; implement + test. **Done (2026-06-01) — option 5 (owner choice):** `RunRegistry.list_active()` + `GET /api/runs/active` (run_id / session_id / started_at / first-user-text as a provisional title; registered before `{run_id}` so the literal path wins — pinned by a route-resolve test) return in-flight runs; `SessionRecents` merges them as **display-only** "in progress" rows for sessions not yet in the DB list, and the spinner reflects the active set. Because the synthetic rows are *not* injected into the `sessions-table-all` cache, `useActiveSessionTitle` keeps reading the canonical DB rows — so once the run completes and the session lands in `state.db` with its **LLM-generated title**, its id enters `dbSessionIds`, the synthetic row drops, and the real row (LLM title) takes over with no flicker/dup. Pinned by `test_runs.py` (+2). pyright 0 / pytest 386 / typecheck / lint / vitest 97 / build / e2e 4 green. **Follow-up (2026-06-02):** the provisional title (first user text) wasn't showing during a *live* session — the **optimistic** prepend (`useRunsStream.sendMessage`), not the synthetic row, renders then, and it set no title. Now it sets `title = prompt.trim().slice(0,80)`, so the first-user-text shows immediately and the run's auto-title replaces it on completion (sessions-cache refetch). **Follow-up 2 (2026-06-02):** the title still flashed prompt → "Untitled session" → LLM title (the post-completion refetch lands a still-title-less DB row; auto-title is async) and concurrent runs' titles didn't auto-update. Replaced the optimistic-row title with a `provisionalTitles` store (sessionId → first prompt) used as a render fallback in `SessionRecents` + `useActiveSessionTitle` (a title-less DB row shows the prompt, never "Untitled"); and the single 6s post-completion refetch is now staggered (now / 3s / 8s / 18s) so the LLM title appears on its own — incl. concurrent runs — without a click/refresh. **Follow-up 3 (2026-06-02):** owner still saw a brief "Untitled" — the `/sessions` table (`SessionsPanel`) lacked the fallback, and the store was in-memory so a refresh during the window lost it. Bulletproofed: `provisionalTitles` is now **persisted** (capped ~60) so it survives refresh; the fallback is applied in `SessionsPanel` too; and `useActiveSessionTitle` additionally falls back to the active session's **first user message** (always in the store). | done (2026-06-02) |
| **D36** | Stability cluster: rate-limit too low, single-session spinner, fragile "Dashboard unavailable", inert Retry | `server/middleware/rate_limit.py`, `server/lib/config_reader.py`, `server/app.py`, `src/components/chat/SessionRecents.tsx`, `src/components/layout/ErrorBoundary.tsx` | Owner-reported prod/dev instability: (a) flat **100 req/60s per-IP** — on localhost every request shares one bucket and the SPA fans out dozens per load, so a few refreshes hit `rate_limit_exceeded`; (b) the Recents spinner keyed off the single `activeRunId`, so concurrent runs didn't all spin; (c) the `/api/sessions` query was `retry:false`, so one blip (often the 429) stranded "Dashboard unavailable"; (d) "chat encountered an error" + Retry appeared inert. | **Done (2026-06-01):** (a) rate limit is now **config-driven + loopback-aware** — trusted loopback (the single-user norm, already trusted by auth) gets a generous cap (default 3000/min), remote peers stay strict (240/min); both via `config_reader` (`HMS_RATE_LIMIT*` / config.yaml `extra`). (b) spinner keyed off `runningBySession` → every running session spins. (c) sessions query `retry:1`. (d) `ErrorBoundary` Retry bumps a `resetKey` to force a clean remount. Pinned by `test_security.py` (+2). typecheck / lint / vitest 97 / build / e2e 4 / pyright 0 / pytest 384 green. **Needs runtime repro:** the exact "chat encountered an error" trigger (suspected the 429 cascade — likely resolved by (a); confirm live, else capture the stack). **Follow-up (2026-06-02) — spinner lingered after completion:** it keyed off the 5s-polled `/api/runs/active` set, so a just-completed run kept spinning until the next poll (clicking forced a re-render). Now keyed off `inflightSessionIds` (synthetic rows = active − DB) ∪ `runningBySession` — a completed run enters `dbSessionIds` (out of inflight) and this client's `runningBySession` clears on `run.completed`, so it stops at once. | done (2026-06-02; (d) trigger needs repro) |
| **D37** | Non-default-profile session data: messages don't load (#2) + duplicate rows after wrong-home restart (#3) | `server/routes/chat.py` (`get_session_messages`); `server/runs.py` / `lifecycle.py` (D31 wrong-home) | Owner-reported once D29/D30 landed: **(#2)** Recents / `/sessions` now *list* non-default-profile sessions (D29), but **clicking one fails to load its messages** — `GET /api/sessions/{id}/messages` still reads only `db()` (default home), not the session's profile `state.db`. **(#3)** after `hms restart` under a non-default profile (D31 wrong-home), the same session shows **twice** in Recents / `/sessions` (tagged `default` *and* `creative`, but both are actually creative's) — the wrong-home gateway wrote the session into the default DB as well, so the cross-home aggregation reads it from both. Both are profile-data-correctness issues entangled with the view-scope model. | **Suspended (owner): investigate + design together with D28 (profile view scope) + D31 (wrong-home) before implementing**, so the message-read path, the dedup, and the scope model are designed coherently (avoid piecemeal logic bugs). Sketch: (#2) `/api/sessions/{id}/messages` reads the row's `profile` DB (the list already tags `profile`); (#3) dedup by `session_id` in the aggregation + fix the wrong-home double-write at its source (D31). | open (P2 — suspended; with D28 + D31) |
| **D38** | LLM stream bleeds across sessions | `src/store/chat.ts` (`setActiveSession`), `src/hooks/useRunsStream.ts` (mount-resume) | Owner-reported: session #1 done, session #2 running; entering #1 from `/sessions` then "open chat" back to `/chat` showed **#2's live LLM stream inside #1**. Root cause: `setActiveSession` cleared `messages` but left `activeRunId`/`activeTurnId` pointing at the *previous* session's run; the switch happened while `useRunsStream` was unmounted (on `/sessions`), so its session-switch effect never reset it — then `/chat` re-mounted and the mount-resume re-attached the stale (session #2) run, streaming its deltas into session #1's transcript. | **Done (2026-06-02):** `setActiveSession` now re-points `activeRunId`/`activeTurnId` at the NEW session's own in-flight run (`runningBySession[id] ?? null`, else clear); the mount-resume adds a **stale-run guard** — only resume a run that belongs to the active session, else clear it. Pinned by `chat.test.ts` (+1). typecheck / lint / vitest 98 / build / e2e 4 green. | done (2026-06-02) |
| **D39** | Switching between concurrent running sessions loses content + bleeds | `src/hooks/useRunsStream.ts` (session-switch effect, `attachRun`, `detach`) | Owner-reported (follow-on to D38, multi-run): switching back and forth between sessions that are both running shows the **user prompt vanishing + the assistant half-cut**, occasionally very slow load, and live content **jumping between sessions**. Two distinct defects beyond D38's store fix: **(a) content-loss** — the session-switch effect did `setActiveRunId(null)`/`setActiveTurn(null)` *before* its async live-run re-verify; D38 had already pointed `activeRunId` at the new session's run, so the null re-opened the exact window where `reconcileSession` (ChatPanel) sees no active run and wipes the in-flight turn. **(b) bleed** — `attachRun`'s async transcript-seed had no supersession guard: a rapid B→A→B switch let run A's seed resolve after `activeTurnId` was already B, so `appendDelta(A.partial)` landed in B's bubble, and its late `subscribe(run:A)` leaked A's channel into the global `subscriptions` map (replayed on every reconnect). | **Done (2026-06-02):** (a) the session-switch effect no longer nulls `activeRunId`/`activeTurnId` (it trusts `setActiveSession`'s re-point) — it only detaches the old subscription, then re-verifies + re-attaches the new session's run; a stale entry clears the run only if it's still the active one. (b) added a generation counter (`attachGenRef`, bumped in `detach`/`attachRun`): `attachRun`'s async seed captures its generation and **bails before any store mutation or `subscribe`** if a newer attach/detach superseded it. Pinned by `chat-reconcile.test.ts` (+1, the switch→reconcile no-content-loss invariant). typecheck / lint / vitest 99 / build / e2e 4 green. | done (2026-06-02) |
| **D40** | Sessions preview drawer doesn't live-sync a running session | `src/panels/SessionsPanel.tsx` (`PreviewDrawer`), `src/hooks/useLivePreview.ts`, `src/lib/live-preview.ts` | Owner-reported: opening a *running* session's preview drawer from `/sessions` shows a static snapshot — it doesn't stream like `/chat`. Root cause: the drawer renders a one-shot `GET /api/sessions/{id}/messages` (persisted DB rows only; upstream persists the turn on completion, so an in-flight run's prompt + partial aren't there) with no WS subscription to `run:<id>`. **Approaches:** (1) ⭐ **true real-time** — a read-only `useLivePreview(sessionId)` hook that, when the session has an active run (`/api/runs/active`), seeds from `/api/runs/{id}/transcript` and subscribes to `run:<id>` into **local** state (not the global chat store, so it can't cross-contaminate the active `/chat` session), appending the in-flight turn after the DB history; (2) **poll** the transcript every ~1–2s while running — simpler, ~1–2s lag; (3) leave the drawer static and rely on "open in chat". | **Done (2026-06-02) — approach 1 (owner choice, WS real-time):** new `useLivePreview(sessionId)` finds the session's in-flight run via the shared `["runs-active"]` query, seeds from `/api/runs/{id}/transcript`, and subscribes to `run:<id>`, accumulating deltas/reasoning/tools into **local** component state (the drawer appends them after the DB history). It's fully isolated from the global chat store, so it can't contaminate `/chat`. Seq-dedup (`shouldApplyFrame`, seeded from the transcript seq) drops ring-replay duplicates; on `run.completed/failed/cancelled` it clears the live mirror + invalidates the preview/active queries so the persisted turn reloads from the DB. Pure reducers extracted to `src/lib/live-preview.ts` (mirrors `run-events.ts`). Pinned by `live-preview.test.ts` (+11). typecheck / lint / vitest 110 / build / e2e 4 green. | done (2026-06-02) |
| **D41** | Pinned sessions aren't server-persisted (per-browser only) | `src/hooks/usePinnedSessions.ts`, `server/lib/station_prefs.py`, `server/routes/preferences.py`, `server/app.py` | Owner-reported: sidebar **Pinned** differs across browsers/devices because it lives only in `localStorage` (`hms:pinned-sessions`), so it isn't "truly persistent". **Audit (other browser-persistence):** Pinned is the *only* piece of user **content** state kept client-side; everything else is genuinely device/viewport UI preference where per-browser is correct — theme/skin/font (`store/app.ts`), language (`i18n`), sidebar module (`Sidebar`), panel widths (`PanelTwoColumn`/`WorkspaceContextPanel`), settings tab, workspace-open, files show-hidden, setup-dismissed; the zustand `hms-chat-prefs` store holds per-tab runtime (`activeRunId`/`runningBySession` — MUST stay local) plus device prefs (model/effort). So only Pinned should sync. **Approaches (storage/scope):** (1) ⭐ Station-owned JSON sidecar under the data dir behind `GET/PUT /api/preferences/pinned`; server is source of truth, `localStorage` becomes a fast-start cache. Scope: owner-level vs per-profile. (2) state.db table — rejected (upstream-owned schema). | **Done (2026-06-02) — approach 1, owner-level (owner choice):** new `server/lib/station_prefs.py` (a key→value JSON sidecar at `hms_data_dir()/preferences.json`, atomic tmp+rename, dedup/clean/cap, structured for future synced prefs) + `server/routes/preferences.py` (`GET`/`PUT /api/preferences/pinned`; CSRF via the global middleware). Owner-level (not per-profile): the sessions list aggregates across profiles, so a pin is a globally addressable id. `usePinnedSessions` now treats the server as source of truth — `placeholderData: readCache` gives an instant first paint from the localStorage *cache*, the GET confirms, and `toggle` is a canonical optimistic mutation (cancel in-flight read → patch cache + cache-file → PUT → reconcile / rollback on error). Pinned by `test_preferences.py` (+11, lib + route incl. CSRF). pytest 398 / pyright 0 / typecheck / lint (no-hardcoding + no-inline-style) / vitest 110 / build / e2e 4 green. | done (2026-06-02) |
| **D42** | Chat Markdown lacks math (KaTeX) + Mermaid diagrams | `src/components/chat/ChatBubble.tsx`, `src/components/chat/MermaidDiagram.tsx`, `package.json` | From the agent-web-client spec (§1) audit: the markdown pipeline is `react-markdown` + `remark-gfm` + `highlight.js` only — LaTeX math and ` ```mermaid ` diagrams render as raw text, which agents emit frequently. | **Done (2026-06-02):** added `remark-math` + `rehype-katex` (+ `katex` CSS) to `ChatBubble`'s single canonical `MarkdownText` pipeline (covers `/chat` **and** the `/sessions` preview, both route through `ChatBubble`); the `pre` handler routes ` ```mermaid ` blocks to a new `MermaidDiagram` that **dynamically imports** `mermaid` (own 589 kB chunk, loaded on demand — base bundle untouched), `parse`s first so a mid-stream/partial diagram falls back to source instead of throwing, renders with `securityLevel:"strict"`, and is theme-aware. KaTeX rides the lazy chat chunk (+~280 kB, `/chat`-only). tsc 0 / eslint 0 / no-inline-style / no-hardcoding / vitest 110 / build (mermaid split confirmed) / e2e 4 green. **Runtime-verify** a math + a mermaid message. *Note:* single-`$` inline math is on (full LaTeX); if currency `$…$` false-positives annoy, flip `remark-math` `singleDollarTextMath:false` (follow-up). **Follow-up (2026-06-02):** Mermaid no longer flashes its source while loading/streaming — `MermaidDiagram` shows a compact "Rendering diagram…" placeholder and only reveals the source once the code stays invalid for ~1s (i.e. streaming finished AND it genuinely won't parse), instead of rendering the `pre`/code fallback throughout the load. **Follow-up 2 (2026-06-02):** (a) wide diagrams render at **natural size**, centered + horizontally scrollable (not shrunk to the column) — `naturalSizeSvg` (shared `src/lib/mermaid-util.ts`) promotes mermaid's `max-width:Npx` to the actual width; (b) `suppressErrorRendering:true` stops mermaid injecting the "Syntax error" bomb graphic into the page bottom during streaming (owner Gantt repro) — `render()` throws and the settle logic handles it. | done (2026-06-02) |
| **D43** | Message ops: edit-and-resend / regenerate / real branch | `src/components/chat/ChatBubble.tsx`, `src/lib/branch.ts`, `src/store/chat.ts`, `src/hooks/useRunsStream.ts`, `src/panels/ChatPanel.tsx` | Spec §1. "Fork" only seeded a *new empty* chat with the message text; no history branch, no edit-and-resend on user bubbles, no regenerate on assistant bubbles. | **Done (2026-06-02) — frontend-only (no backend change):** `POST /api/runs` + `start_run` **already accept `conversation_history`** (used end-to-end at runs.py:511), so a branched run seeds the agent with prior turns directly — no upstream symbol, no capability gate. Three bubble ops, all creating a **new session seeded with truncated prior context** (state.db can't be per-message-truncated — only `delete_session` is public — so we branch *forward*, not rewrite in place): **Edit & resend** (user) prefills the edited prompt with context before it; **Regenerate** (assistant) re-asks the producing prompt and **auto-sends** (one-click, via a `pendingAutoSend` + ChatPanel effect); **Branch from here** (any) continues with context through that message. Pure `src/lib/branch.ts` (`buildBranchHistory` strips tool cards + empties, `precedingUserIndex`, `messagePlainText`) — unit-tested. Composer prefill moved from mount-only `sessionStorage` to a reactive `composerDraft` store field (works on `/chat` too). Ops hidden in the read-only `/sessions` preview (message not in the live transcript). Pinned by `branch.test.ts` (+7). tsc 0 / eslint 0 / no-inline-style / no-hardcoding / vitest 131 / build / e2e 4 green. **Runtime-verify** edit/regenerate/branch. **Follow-up candidate:** the branched session's *displayed* transcript starts fresh (inherited context is agent-side only) — a "branched from … / show inherited context" affordance would close the loop. **SUSPENDED (owner, 2026-06-02):** owner found several bugs in runtime testing — the branch/edit/regenerate logic needs reconsideration before this ships. Implementation is on `main`-pending but parked; revisit the model (new-session-vs-in-place, the auto-send timing, the displayed-vs-agent context gap) when resumed. | suspended (2026-06-02; owner-reported bugs — rework) |
| **D44** | Memory sovereignty UI (structured memory store) | `src/panels/MemoryPanel.tsx`, `server/routes/memory.py`, `server/lib/upstream_shim.py` (`_Memory`), `server/lib/upstream_paths.py` | Spec §3 ("数据主权与可解释性的关键"); promotes coverage-doc §9 #4 / D12. Station surfaced SOUL.md + memory *files* but not the **structured** memory store — i.e. you couldn't *view / delete what the agent remembered*. | **Done (2026-06-02) — MVP, holographic-scoped.** **Audit:** upstream memory is **pluggable** (hindsight/honcho/mem0/supermemory/… — mostly remote, no uniform CRUD); the active **holographic** provider keeps a local `$HERMES_HOME/memory_store.db` with a real CRUD API (`list_facts` / `remove_fact`) — the one provider with a local surface. Built on that, degrading cleanly otherwise. **Backend:** shim `_Memory.MemoryStore` (capability-gated `_try_import("plugins.memory.holographic.store","MemoryStore")` — the only door), `upstream_paths.memory_store_path(home)`, `CapabilityFlags.memory_store`, and `GET /api/memory` + `DELETE /api/memory/{id}` (profile-scoped to the *active* profile; never opens/creates the db when absent; CSRF on delete). **Frontend:** `/memory` route (manage nav, Brain icon) + `MemoryPanel` — fact cards (content · category · tags · trust% · used N× · date) with **Forget** (confirm) — and a clean "not available" state for remote providers; `useMemory` hook; `memory` i18n (en+zh). Pinned by `test_memory_route.py` (+6). pytest 404 / pyright 0 / ruff / tsc / eslint / no-inline-style / no-hardcoding / vitest 131 / build / e2e 4 green. Deepens **D9** by one symbol. **Runtime-verify** on a holographic profile. **Follow-ups:** report the active provider name (so remote-provider users get a precise note); surface `record_feedback` (👍/👎 trust) + edit. **Follow-up (2026-06-02) — moved into Profile (owner):** the standalone `/memory` route reused a **retired** path (its old MEMORY.md/USER.md function had already migrated into ProfilePanel) and sat at the wrong level. Moved to a **"Memory store" tab inside ProfilePanel** (beside SOUL.md/MEMORY.md/USER.md), **scoped to the selected profile** (`GET`/`DELETE /api/memory?profile=<name>` → `resolve_profile_home`, default→process home); removed the route + nav + `MemoryPanel`, extracted `src/components/profile/MemoryFacts.tsx`. Matches coverage §7 ("memory belongs in Profile"). pytest / pyright / tsc / eslint / vitest 131 / build / e2e 4 green. | done (2026-06-02) |
| **D45** | Agent execution richness: plan/todo + live subagent + steer | `src/lib/ws-types.ts`, `server/runs.py`, `src/components/chat/*`, `src/panels/GroupPanel.tsx` | Spec §2 — its headline differentiator. The `/ws` vocab is **8 run events only** (no plan / spawn / steer); `GroupPanel` reconstructs subagent structure from session parent/child rows (REST), not a *live* execution swimlane; there is no mid-task steer. | **Approach (strongly upstream-gated; the "refactor when necessary" locus):** agree **3 versioned frames** with upstream (`plan.updated` / `subagent.spawned` + `.delta` / `run.steer`), add capability flags + renderers (progress bar / swimlane / non-interrupting steer box). This **upgrades the run event model from chat-frames to execution-frames — not a rewrite.** Merge with **D34**; the run-status/protocol-maturity prerequisite stays tracked under **D2** (+ `PROJECT_CONSTITUTION` §3). | open (P2; with D34/D2 — blocked on upstream events) |
| **D46** | Command palette (Cmd-K action launcher) | `src/components/search/CommandPalette.tsx`, `src/lib/commands.ts`, `src/components/layout/AppShell.tsx`, `src/i18n/*` | Spec §5. Cmd-K opened **global search only** (nav + message FTS via the old `GlobalSearch`, whose nav list was a hand-maintained `NAV_ENTRIES` array that drifted from `ROUTES`); no "run any action / navigate / toggle" launcher. | **Done (2026-06-02):** promoted `GlobalSearch` → `CommandPalette` (one ⌘K surface: **Actions** + **Pages** + **Messages**). New pure `src/lib/commands.ts` derives nav commands from the single `ROUTES` table (kills the `NAV_ENTRIES` drift) + a `filterCommands` fuzzy match — both unit-tested. Actions: New chat / Toggle theme / Toggle reasoning / Toggle token labels (closures over the live stores). Grouped sections with headers, arrow/enter/escape + hover selection; message FTS unchanged. New `palette` i18n namespace (en + zh). Agent slash-commands stay in the Composer `SlashMenu` (no duplication). Pinned by `commands.test.ts` (+6). tsc 0 / eslint 0 / no-inline-style / no-hardcoding / vitest 116 / build / e2e 4 green. **Runtime-verify** ⌘K → actions/nav/search. **Follow-up (2026-06-02):** (a) smooth open/close — `closing` state + 200ms delayed unmount, top-anchored `hms-palette-in/out` keyframes (the shared `slideUp/Down` re-center vertically, which would yank the palette); (b) larger surface (640 wide, 440 list); (c) **fixed message search** — it queried the dead `/api/dashboard/sessions/search`; now hits `/api/search` (real FTS, `{results:[{session_id,message_id,snippet}]}`) and a hit opens its conversation in `/chat`. **Follow-up 2 (2026-06-02):** ⌘K now **toggles** (AppShell opens; the palette closes itself via the animated `requestClose` on a document-level ⌘K/Esc listener), and arrow-key navigation **auto-scrolls** the selected row into view (`scrollIntoView({block:"nearest"})`). | done (2026-06-02) |
| **D47** | Observability inspector (raw WS/REST frames + TTFT + replay) | new `src/components/dev/*`, `src/store/ws.ts` | Spec §9 (production-grade). Frames already flow through `ws.ts`; there's no raw-frame inspector, no TTFT metric, no session replay. | **Approach (pure client, dev-gated):** a dev-only drawer subscribing to a ring buffer of `lastMessage`; TTFT = first `message.delta` − POST time; replay = re-emit the ring by `seq`. | open (P3) |
| **D48** | Re-enable PWA (service worker / offline shell / install / push) | `src/main.tsx`, `public/manifest.webmanifest`, `vite.config.ts` | Spec §7. `main.tsx` **actively unregisters** service workers; `manifest.webmanifest` + icons exist but with no SW there's no offline / install-to-home / push. | **Approach (pure client):** Workbox precache the app shell + offline fallback; push for long-task completion / approval-needed can reuse the **Telegram gateway** as a supplementary channel (spec §7). Re-introduce carefully (cache-busting the in-process plugin's dist). | open (P3) |
| **D49** | PDF export + TTS playback | `src/panels/SessionsPanel.tsx`, `src/components/chat/*`, new hook | Spec §6. Export is JSON/MD only (no PDF, incl. tool calls for audit); STT exists (`useVoiceInput`) but there's no TTS. | **Approach (pure client):** PDF via a print stylesheet or a client lib (full transcript incl. tool calls); TTS via the browser `SpeechSynthesis` API (pairs with the existing STT). **Done (2026-06-02):** PDF — `src/lib/export-pdf.ts` builds a standalone printable HTML doc (all roles incl. tool rows, HTML-escaped against injection) and opens a print window → browser "Save as PDF" (zero-dependency); `SessionsPanel` gains a PDF button beside JSON/Markdown. TTS — new `useTextToSpeech` (browser `SpeechSynthesis`, single global queue so one message speaks at a time; degrades to unsupported where the API is absent) drives a Speak/Stop button on each `ChatBubble`. Pinned by `export-pdf.test.ts` (+5, incl. HTML-injection escaping). tsc 0 / eslint 0 / no-inline-style / no-hardcoding / vitest 121 / build / e2e 4 green. **Runtime-verify** the PDF print dialog + Speak button. **Follow-up (2026-06-02) — rich rendering + more entry points:** the PDF was a raw-text dump (Markdown/Mermaid unrendered, Chinese blank, tool JSON included). `export-pdf` (now `.tsx`) renders each transcript like the chat — **Markdown (GFM) + KaTeX math as MathML (no font dependency in the detached print window) + Mermaid diagrams** — with **tool-call cards stripped** (`historyToChatMessages` → text segments only) and a **CJK-capable font stack** (fixes blank Chinese); the heavy renderers (`react-dom/server`, `react-markdown`, `mermaid`) are dynamically imported (own chunks). Export now also lives on the **ChatTitleBar ··· menu** and each **Recents row ··· menu** (single-session) beside JSON/Markdown. `export-pdf.test.ts` rewritten (pure fns: tool-strip, mermaid-extract, CJK-doc, injection escaping). vitest 124. | done (2026-06-02) |
| **D50** | Centralize transcript reconciliation | `src/hooks/useRunsStream.ts`, `src/store/chat.ts` | `ARCHITECTURE.md` §6: the "clear the trailing streaming bubble" operation is duplicated across **≥4 control-flow paths** (reconnect guard, terminal frame, resume-on-mount, stop) — touched in D38/D39, correct today but fragile. | **Approach (pure client refactor):** extract a single helper/reducer that every path (hook + store) calls; pin with chat-store tests so the consolidation is behaviour-preserving. | open (P2) |
| **D51** | Profile personality picker (`agent.personalities` overlays) | `src/components/profile/Personalities.tsx`, `src/panels/ProfilePanel.tsx`, `server/routes/profiles.py` | Profile gap **G1** / coverage §7 #3 / D12: the per-profile `agent.personalities` overlays were discoverable only via the `/personality` slash autocomplete — no UI. | **Done (2026-06-02):** new `GET /api/profiles/{name}/personalities` (reads the *selected* profile's config.yaml `agent.personalities`, normalizing the str / `{description, system_prompt}` shapes) + a **"Personality" tab in ProfilePanel** (grouped with SOUL as persona, before the USER/MEMORY/store memory layering) listing each overlay (name · description · expandable prompt) with a one-click **copy `/personality <name>`** to apply it in chat. Read-only by design — the *active* overlay is a runtime, per-chat choice (no persisted "active" upstream). Pinned by `test_profile_config.py` (+2). pytest 406 / pyright 0 / ruff / tsc / eslint / no-inline-style / vitest 131 / build / e2e 4. **Follow-up:** a Composer pill to *switch* the active overlay in chat (the runtime side). | done (2026-06-02) |
| **D52** | Profile export / import (archive) | `server/routes/profiles.py`, `server/lib/upstream_shim.py`, `src/panels/ProfilePanel.tsx` | Profile gap **G2** (spec §10 portability / data sovereignty): upstream `export_profile` / `import_profile` (tar archives, with safe-extract guards) aren't surfaced — no backup / migrate / share. | **Approach:** shim-expose `export_profile` / `import_profile`; `GET /api/profiles/{name}/export` (stream the archive download) + `POST /api/profiles/import` (multipart upload → new profile, reusing upstream's `_safe_extract_profile_archive`); overview **Export** button + profile-list **Import**. Export-only is a fine first slice. **Done (2026-06-02):** shim `_Profiles.export_profile` / `import_profile` (capability-gated). `GET /api/profiles/{name}/export` runs `export_profile` to a tempdir and streams the `.tar.gz` (`Content-Disposition: attachment`); `POST /api/profiles/import` (multipart `file` + optional `name`, 256 MB cap, CSRF) writes the upload to a tempfile and runs upstream's safe-extract `import_profile` → new profile (`FileNotFoundError`/`ValueError` → 400). Frontend: an **Export** button in the Profile overview (same-origin anchor download, carries the auth cookie) + an **Import** button in the side-list header (hidden file input → multipart upload → invalidate `["profiles"]` + select the new one). `profile.export` / `profile.importProfile` i18n (en+zh). Pinned by `test_profile_config.py` (+5: export stream, 503-degrade, import roundtrip, no-file, CSRF). pytest 411 / pyright 0 / ruff / tsc / eslint / no-inline-style / no-hardcoding / vitest 131 / build / e2e 4. | done (2026-06-02) |
| **D53** | Per-profile gateway service (start/stop a profile's own gateway) | `server/routes/profiles.py` / `server/lifecycle.py`, `src/panels/ProfilePanel.tsx` | Profile gap **G3**; retires the **D31** wrong-home-restart class. Upstream registers a per-profile gateway *service* (`_maybe_register/unregister_gateway_service`) + per-profile start/stop, but Station's gateway lifecycle targets the single active home. | **Approach:** per-profile **Start/Stop** in the Profile overview (the D31 "per-profile services" approach #1) — stop the default gateway, start the selected profile's own (`hermes -p <name> gateway start`, each with its own plist/home); capability-gated; sidesteps the in-process loop-guard. Alt: detect-and-warn only. | open (P2, with D31) |
| **D54** | Dev backend dies when a host-venv dev dep (watchdog) is missing | `server/cli.py`, `pyproject.toml` | After the host venv was rebuilt for hermes-agent **0.15.1**, Station's `[dev]` extra (watchdog) was dropped; `hms dev` → `python -m server dev --reload` then `sys.exit(2)` **before** binding the Unix socket, so Vite proxied to a missing socket (`connect ENOENT …/station-dev.sock`). A dev-only convenience took down the whole proxy — violates "degrade, never assume". | **Done (2026-06-03):** `_run_with_reload` now **degrades to a single no-reload `_run_once`** (binds the socket anyway, warns how to restore live reload) instead of exiting; reinstalled `pip install -e '.[dev]'` into the host venv (watchdog 6 / pyright 1.1.410). Verified: socket binds ~2 s and stays up, `/api/auth-status` → 200 over the socket. Pinned by `test_cli.py` (+1: missing-watchdog degrades to `_run_once`, no SystemExit). | done (2026-06-03) |
| **D55** | Model picker lacks Thinking / effort (ReasoningPicker is a separate composer pill) | `src/components/chat/composer/ReasoningPicker.tsx`, model picker, `src/components/chat/Composer.tsx` | Upstream desktop (`apps/desktop`) folds reasoning **into the model menu**: each model row is a hover submenu with a **Thinking** switch + **Effort** radio (Minimal / Low / Medium / High / Max) + **Fast** toggle, capability-gated per model (only `reasoning`-capable models show effort; `fast` via a `speed=fast` param or a `-fast` sibling). Station's standalone ReasoningPicker shows effort even when unsupported and isn't tied to the chosen model. | **Approach:** refactor ReasoningPicker into the model selector as a per-model submenu (Thinking + Effort + Fast), capability-gated via shim `models.get_capabilities`; the active row shows a live "Fast · Med" meta; persist through the existing reasoning-effort path; retire (or compact-mirror) the standalone pill. Optional: an "Edit Models…" picker-visibility curation. **Done (2026-06-04):** the standalone ReasoningPicker was absorbed INTO the model picker — a "Thinking" effort row (None / Default / Minimal / Low / Medium / High / Max) sits at the top of the model dropdown and the model pill shows a live "· Med" effort suffix; persists through the existing reasoning path; `ReasoningPicker.tsx` deleted. Station has no per-model capability source, so effort shows for every model (no regression — the old pill did too); per-model gating is a follow-up if `models.get_capabilities` gets surfaced. tsc / eslint / no-inline-style / no-hardcoding / vitest 136 / build / e2e 4. | done (2026-06-04) |
| **D56** | No voice input in the composer (dictation / STT) | `src/components/chat/Composer.tsx`, new transcription door in `server/routes/` | Upstream desktop composer has a **dictation** button (mic → transcribe → insert text; idle / recording / transcribing) plus a context-aware primary button (empty → start voice; text → send). Station has neither. | **Approach:** add a composer **mic/dictation** button (MediaRecorder → upload → transcription → insert into the draft), capability-gated. **Depends on a transcription (STT) backend** — first audit whether the gateway/provider exposes STT through the shim; if absent, degrade by hiding the button. Full realtime voice-conversation (TTS + barge-in) is out of scope. **Note (2026-06-04):** Station's Composer **already has a Web Speech API mic** (live browser dictation into the draft) — so basic dictation exists. What remains from the desktop is the **upload → server-side STT** path (provider-grade transcription, independent of the browser engine) plus the context-aware voice primary button; both still gated on a shim STT door. | open (P3; browser-mic done, server STT pending) |
| **D57** | No Artifacts view (a session's images / files / links aren't collected) | new `src/panels/ArtifactsPanel.tsx` (+ route), reuses session messages | Upstream desktop derives an **Artifacts gallery purely from session messages / tool outputs** — regex-extracts markdown images, links, URLs and file paths, classifies image / file / link, then renders a filter bar + paginated table + image previews + open-in-chat. No new storage — a projection over existing messages (matches Station's "chat store is a projection" model). | **Approach:** new Artifacts panel + route that scans the active (or view-scoped) session's messages, extracts and classifies artifacts, and renders a filterable, paginated gallery with previews and jump-to-message; serve images/files through the existing upload/file routes. Pure client projection. | open (P2) |
| **D58** | Session actions are split and have no right-click; the ChatTitleBar ··· menu is bespoke | `src/components/chat/ChatTitleBar.tsx`, session-list rows, new `SessionActionsMenu` | Upstream desktop renders **one** `useSessionActions` source as BOTH a ··· dropdown and a right-click context menu — Pin / Copy ID / Export / Rename / Archive / Delete, each capability-gated, Rename via a dialog. Station's title-bar menu and session-row actions are separate and duplicated, with no right-click. | **Approach:** extract a single `SessionActionsMenu` whose dropdown and context-menu surfaces share one item spec (Pin / Copy ID / Export / Rename / Archive / Delete); use it from the session title ··· and from session-list rows (right-click); replace the bespoke ChatTitleBar ··· menu. **Done (2026-06-04):** new `src/lib/session-actions.tsx` `buildSessionActions` is the single source (ordered, i18n-labelled, capability-gated: Rename / Pin / **Copy ID** / Export JSON·MD·PDF / Clear local / Archive / Delete); a new `SessionActionsMenu` dropdown (with an inline Rename dialog) renders it for the ChatTitleBar ···, and the SessionRecents right-click menu now renders the same builder — both surfaces share one spec. ChatTitleBar gained Copy ID / Pin / Archive / Delete + dialog rename (was `window.prompt`). Pinned by `tests/unit/lib/session-actions.test.ts` (+5). tsc / eslint / no-inline-style / no-hardcoding / vitest 136 / build / e2e 4. | done (2026-06-04) |
| **D59** | No Appearance settings tab; no Tool Call Display mode; Language grouped with display | `src/panels/SettingsPanel.tsx`, Preferences, theme + tool-view stores | Upstream desktop's **Appearance** tab groups **Color Mode** (light / dark / system), **Tool Call Display** (Product = concise summaries vs Technical = raw args/results), and **Theme** (palette previews), while **Language** lives under Preferences. Station has no Tool Call Display mode and groups Language with display prefs. | **Approach:** add an **Appearance** tab (Color Mode + Theme + a new **Tool Call Display** Product/Technical toggle backed by a `$toolViewMode`-style store the chat tool blocks read) and **move Language to Preferences**. Tool Call Display is a genuinely new capability (collapse raw tool payloads by default). **Done (2026-06-04 — colours/approach 1:1 from upstream desktop, per owner):** the Appearance tab now reads **Color Mode** (relabelled from "Theme") → **Tool Call Display** (new) → **Theme** (relabelled from "Skin") → Font size; Tool Call Display is two desktop-style Product/Technical cards backed by a persisted `useToolViewStore` (`hms_tool_view`, defaults **Product**) that `ToolCallCard` reads — Product hides the raw ARGS/RESULT payloads (no expand), Technical shows full detail; copy mirrors upstream 1:1. **Language moved to Preferences** (new Globe section). tsc / eslint / no-inline-style / no-hardcoding / vitest 136 / build / e2e 4. | done (2026-06-04) |
| **D60** | `/group` route + "Group" naming (owner wants Agents) | `src/routes/registry.tsx`, `src/panels/GroupPanel.tsx`, `src/i18n/{en,zh}.ts` | Upstream desktop calls this surface **Agents** — a live subagent **spawn-tree** for the turn (parent → child delegations with per-agent model / duration / tools / tokens / cost / files). Station's equivalent is `/group` labelled "Group" / "协作" (multi-session parent → child). Owner wants route `/agents` + English **Agents**; Chinese stays **协作**. | **Owner directive (2026-06-04) — not just a rename:** **refactor GroupPanel → AgentsPanel and reuse upstream desktop's Agents feature** (the live subagent **spawn-tree**: parent → child delegations streaming progress, each row carrying model / duration / tools / tokens / cost / files read·written, with grouped delegations and a roll-up summary). Step 1 (quick): rename route `/group` → `/agents` + `labelKey` / en "Agents" (zh "协作" unchanged) + redirect `/group`. Step 2 (larger): port the spawn-tree onto Station's run/subagent stream — **needs a Station-side subagent-activity source** (audit the WS run events for delegation/sub-run frames; today GroupPanel groups whole sessions, not in-turn subagents). **Step 1 done (2026-06-04):** route `/group` → `/agents` (+ `/group` redirect in both shells), `GroupPanel` → `AgentsPanel`, nav label en **Agents** / zh **协作**; the panel already renders the parent→child **session** hierarchy with per-node stats (model / tokens / message count) — Station's available "agents" data. **Step 2 (live in-turn spawn-tree) BLOCKED:** needs subagent/delegation **WS events** Station's closed WS vocab doesn't carry (same gap as **D45**) — fold there. tsc / eslint / no-inline-style / no-hardcoding / vitest 136 / build / e2e 4. **The interactive multi-agent room is now the chosen direction (see D65).** **Rename fully completed (2026-06-04):** beyond the route/label, internal naming is `agents` too — `labelKey: "agents"`, i18n `nav.agents` + the panel's whole `agents:` section (was `group:`); the old `groups` / `groupRoots` / `["group-sessions"]` session-hierarchy code was retired by the room rewrite; stale "Group" panel-name comments fixed. | in-progress (P2; rename done, interactive room → D65, live spawn-tree → D45) |
| **D61** | Architecture direction: JSON-RPC gateway + assistant-ui (desktop) vs in-process shim + own REST/WS + own chat stack (Station) | `ARCHITECTURE.md`, `server/lib/upstream_shim.py`; cross-cutting | **Owner question (2026-06-04):** upstream desktop talks to the agent over a **versioned JSON-RPC gateway** (`HermesGateway extends JsonRpcGatewayClient`) and renders chat with **@assistant-ui/react**; Station is an **in-process plugin** that imports upstream internals through the shim and ships its own REST/WS + chat stack. Which is the better long-term bet as upstream evolves? | **Analysis / recommendation (2026-06-04):** trade-off — *in-process shim* = zero-serialization, full-fidelity access to upstream dataclasses + any Python symbol, but accrues **coupling debt** (every upstream refactor risks the shim; the D9/D12 re-audit cadence exists for exactly this — 0.15.1 moved `state`→`hermes_state`, `runs`→`run_agent`). *JSON-RPC gateway* = a **stable, versioned, decoupled** contract with process isolation + local-or-remote reach, but limited to what the RPC exposes (the dashboard HTTP layer already strips fields like `gateway_running`) and needs the gateway up. **Recommendation: keep in-process as Station's identity/differentiator** (full fidelity, and it *is* the product), but **steer the shim toward upstream's stable / RPC-backed *public* surfaces** and away from privates (`_load_gateway_config`, `_pause_failed_platform`, …): treat the gateway's JSON-RPC method list as the "approved surface", file debt to swap each private reach for a public accessor (upstream one where needed), keep the capability-probe + degrade discipline. **Escape hatch:** if upstream churn accelerates, add the JSON-RPC client as a *fallback transport behind the shim* (in-process when available, RPC when not). Going full-RPC would just make Station a second desktop and forfeit the in-process edge. | open (P3, analysis/decision) |
| **D62** | P1 owner-review refinements (model / title / clear / appearance / streaming) | `src/components/chat/composer/ModelPicker.tsx`, `ChatTitleBar.tsx`, `SessionActionsMenu.tsx`, `ChatBubble.tsx`, `server/routes/chat.py`, `src/styles/theme.css` | Owner review of the P1 batch (7 items): the model picker showed Thinking as a top section (should be a per-model hover) + truncated the model name; the chat title wasn't a click-to-menu trigger and kept a redundant ··· ; action order/labels were off + Clear was view-only; "New conversation" wording; Theme cards were 4-wide + the default skin had no active highlight; "Show reasoning" overlapped Tool Call Display; streaming used a blinking caret. | **Done (2026-06-04):** (1) reasoning moved into a **per-model hover flyout** (Thinking switch + Effort radio Minimal/Low/Medium/High/Max, desktop-style); model pill widened (160→220) + effort suffix dropped. (2) the **title IS the ··· trigger** (click → dropdown, fixed-positioned to escape the bar clip); standalone ··· removed. (3) action order = **Pin / Rename / Copy ID / Export JSON·MD·PDF / Clear Session / Archive / Delete**; **Clear Session now really wipes** via new `POST /api/sessions/{id}/clear` → upstream `SessionDB.clear_messages` (CSRF, 503-degrade; +4 tests). (4) "New conversation" → **New session**. (5) Theme cards → **3-wide (2 rows)**; **root-caused the default-skin highlight bug** — `--hms-accent` was defined for the 5 named skins but NEVER for the default "Hermes Teal" skin, so every `var(--hms-accent)` under it was undefined; added a base `--hms-accent: #ffbd38` (the skin's preview accent dot — **owner: retune if a teal is intended**). (6) removed `showReasoning` (store / Preferences / palette); **reasoning visibility folds into Tool Call Display** (Technical shows chain-of-thought, Product hides). (7) blinking caret → **clock + live m:ss activity timer** that auto-disappears on completion. pytest 442 / pyright 0 / ruff / tsc / eslint / no-inline-style / no-hardcoding / vitest 136 / build / e2e 4. | done (2026-06-04) |
| **D63** | P1 owner-review round 2 (picker / pill / skins / tool-display / clear / search) | `src/components/chat/composer/ModelPicker.tsx`, `src/styles/skins.ts`, `src/styles/skin-bridge.css`, `src/components/chat/ChatStream.tsx`, `server/routes/chat.py` | Second owner pass on the P1 UX: the model dropdown search scrolled with the list; the reasoning flyout's Thinking toggle closed the menu (clicks fell through outside-click) and overflowed the bottom; the pill dropped the effort suffix + truncated names; skins needed Slate (not Rosé) + reorder + Mono default; Tool Call Display wanted Technical-first/default; Clear Session kept the stale title; message-search hits didn't scroll to the message. | **Done (2026-06-04):** (1) model panel restructured — **fixed search header + independently-scrolling list**; the reasoning flyout is **bottom-flush** with the panel and **excluded from outside-click** (new `flyoutRef`) so **Thinking + Effort work**. (2) pill re-adds a compact effort suffix (· Med / · Off), widened to 260 auto-fit, label uses the same `modelLabel` as the rows. (3) skins: **Rosé → Slate** (desktop `#58a6ff`), order **Mono / Slate / Midnight / Ember / Cyberpunk / Teal**, **default = Mono**. (4) Tool Call Display order **Technical / Product**, **default Technical**. (5) **Clear Session resets the title** (backend `set_session_title("")` + client drops the provisional) so the next turn auto-titles fresh. (6) **search jump-to-message** — a `pendingScrollMessageId` store flag → ChatStream scrolls to `data-msg-id` (`hist-<id>` / `hist-run-<id>`) with a flash highlight. **Follow-up (2026-06-04):** the reasoning flyout now opens to the **right** of the panel (was left); the search jump uses **nearest-match** (largest `data-msg-id` ≤ the hit) and **suppresses the auto-scroll-to-bottom while a jump is pending**, so assistant-message hits land correctly (the exact-id match missed mid-run rows → fell to the bottom). pytest 442 / pyright 0 / ruff / tsc / eslint / no-inline-style / no-hardcoding / vitest 136 / build / e2e 4. | done (2026-06-04) |
| **D64** | SPA styles via inline `style={{}}` everywhere — no semantic class layer | `src/**` (cross-cutting), `scripts/lint_no_inline_style.sh` | Owner (2026-06-04): in devtools the Elements tree is generic divs with inline styles — no semantic classNames, so styling is hard to inspect, reuse, theme-override, or maintain, and repeated inline blocks duplicate the same token recipes. Upstream desktop uses Tailwind + semantic classes. | **Approach:** adopt a styling layer — (a) Tailwind v4 (matches desktop) / (b) CSS Modules / (c) a semantic `.hms-*` className vocabulary over the existing token CSS. Incremental: extract the highest-churn components first (ModelPicker / ChatBubble / SessionActionsMenu / SessionRecents) into classes; the existing no-inline-style lint already nudges toward tokens. Large — phase it. | open (P2, large) |
| **D65** | Agents page design direction (spawn-tree vs multi-agent group room) | `src/panels/AgentsPanel.tsx`, new group-room backend; cross-cutting | Owner (2026-06-04) asked for a recommended Agents design, drawing on **two references**: upstream **desktop** (read-only live **spawn-tree** — one agent's in-turn subagent delegations, per-node model/duration/tools/tokens/cost/files, grouped + roll-up) and **EKKOLearnAI/hermes-web-ui** (interactive **multi-agent group chat room** — N **profile-backed** agents, **@mention routing** via per-agent clients, realtime/typing, invite codes, context compression). Station's current Agents page is only a read-only parent→child session-hierarchy tree. | **Recommended (phased):** **(A) now —** polish the existing hierarchy into an "Activity" view (desktop-style per-node stats; pure projection over `/api/sessions`, no new backend). **(B) flagship —** a **Group Room**: pick several **profiles as agents**, **@mention** one to make it reply, routing the turn under that profile's home — **reuses Station's existing per-profile run re-scoping** (`server/lib/profile_run` + the Composer profile pill), so it leans on Station's profile differentiator; needs a modest room/attribution layer + a mention→profile router. **(C) later —** the live in-turn spawn-tree is **blocked on upstream subagent WS events** (D45). **Recommendation: B as the primary direction** (interactive + plays to profiles), with A as the complementary observe tab. **Owner picked B (2026-06-04). MVP slice 1 done:** new `src/store/agentRoom.ts` (roster of profile-agents, persisted) + `AgentsPanel` rebuilt as a room — add/remove profiles as agents, pick a **responder**, and the turn's run is **routed under that profile's home** via a new `sendMessage(..., { profileOverride })` (reuses the per-run `profile` override); the conversation reuses the shared chat store + `ChatStream`, and user turns show **→ @agent** and **assistant replies carry an @agent author badge** (`ChatMessage.agent`; live attribution via a chat-store `agentByRun` map stamped onto the streaming bubble at `turn-<runId>-assistant`). Pinned by `agent-room.test.ts` + `chat.test.ts` (+6). **Isolation rework (2026-06-04, owner review):** the room is now **fully decoupled from /chat** — its conversation lives in `useAgentRoomStore` (own `messages` + run state, **persisted to localStorage**, survives reload) and streams through a dedicated `useAgentRoomStream` (each turn = a real run under the routed member's `profile` + the room's prior turns as `conversation_history`; no shared hermes session, because profile-override runs persist into each profile's own state.db and would fragment one session — runs.py:444). **@mention routing now works** (`parseMention` strips a leading `@member` and routes to it; else the responder chip), with **@ autocomplete** in the room composer + a **Clear room** action. Fixes the shared-state bleed (adding a member pulled /chat in; /agents jumped to /chat's live turn) + the dead @mention. Pinned by `agent-room.test.ts` (+6) + `chat.test.ts`. **Composer reuse done (2026-06-04, owner #1):** the room now renders the shared /chat `Composer` (attach / mic / model picker / slash menu) — gated on a new `running?` prop (the room tracks its own run, not the chat store's `activeRunId`); attachments flow into the agent's run as `ContentPart[]`; model / reasoning come from the shared Composer selection. **Autocomplete + highlight done (2026-06-04, owner #4):** the Composer gained a `mentionNames?` prop → an **@member autocomplete** menu (reuses `SlashMenu` with a `@` prefix) in the /agents room, plus a **syntax-highlight backdrop** (a mirrored div behind a transparent textarea) colouring `/commands` and `@mentions` in BOTH surfaces (`highlightComposerTokens`, +5 tests). **Round-3 fixes (2026-06-04, owner):** room runs now actually stream back — the room ensures the WS `connect()` (only /chat + /sessions did before, so a direct /agents landing never received frames); room runs are hidden from /chat Recents (the room tracks its run session_ids; **Clear room** deletes them server-side) and the Recents active highlight is route-scoped to /chat (no stale highlight on /agents); slash commands **degrade gracefully** (backend 503 `slash_unavailable` + a clear message — they need the running gateway, not standalone `hms dev`); autocomplete is **cursor-aware** (`composerCurrentToken`) so `/` and `@` complete anywhere (multiple @mentions) and insert a trailing space; `@member` routing now matches the first member anywhere, not just the leading token. **Remaining:** a **room list / multiple named rooms** (owner #1) + invite codes; cross-device persistence; Mode C live spawn-tree (→ D45). tsc / eslint / no-inline-style / no-hardcoding / vitest 160 / build / e2e 4; backend 441 passed (one pre-existing `test_state_db` env failure, unrelated → D66). **Round-4 (2026-06-04, owner):** (1) **multi-agent fan-out** — a message routes to EVERY `@member` mentioned (sequential runs; each sees the prior replies via conversation_history; `parseMentions` collects + strips all, deduped), not just the first (which was the bug: trailing mentions were parsed as message text). (2) a refresh mid-run **re-attaches** to the live run (persisted `activeRunId`/`turnAgent`; the WS ring replays the streamed partial) or lands the final of one that finished — the in-flight reply is no longer lost. +multi-mention/resume tests (vitest 161). **Room list / multiple rooms done (2026-06-04, owner #1):** the store is now **multi-room** (`rooms[]` + `currentRoomId` — each room fully isolated: own roster / transcript / run state / session ids; a `_streamRoomId` keeps in-flight frames in the room they started in even across a mid-run room switch; the legacy single-room keys auto-migrate). AgentsPanel gained a **room list** beside the current room (create / select / rename-on-double-click / delete, ≥ 1 room kept). The `s.rooms.flatMap` Recents filter is memoised (a fresh array per render had re-rendered forever). +4 multi-room tests (vitest 165, build, e2e 4). **Remaining:** invite codes; cross-device persistence; Mode C live spawn-tree (→ D45). | in-progress (P2; rooms + multi-agent + resume done — invite codes / spawn-tree remain) |
| **D66** | `test_state_db::test_list_empty` reads real session rows under hermes-agent 0.15.1 | `tests/unit/test_state_db.py`, `server/lib/state_db.py`; cross-cutting | After the 0.15.1 bump the isolated `sessions_client` test reads sessions carrying new billing fields (`billing_base_url`, `api_call_count`) instead of an empty list — the `SessionDB(db_path=tmp)` + `st._singleton` isolation no longer fully stops the route from reaching the real/live db. Confirmed environment-triggered (fails with all owner-round code stashed; passes in a clean checkout / CI). | **Approach:** re-audit the test's db isolation against 0.15.1's `SessionDB` (did the `db_path` arg or default-home resolution change?) so `db()` can't fall back to `$HERMES_HOME` / the real db while a live `hms dev` backend is running. Part of the D9 / D12 version-bump re-audit cadence. | open (P2; clean CI green, local env only) |

## How to use

- **Adding debt:** append a row. Include the file path. If you're leaving
  it knowingly in a PR, link the PR in the row.
- **Paying it down:** flip status to `in-progress`, then `done` with the
  commit. Don't silently delete — a reviewer should see it was retired.
- **Re-audit cadence:** D9 and D12 are re-checked on every hermes-agent
  version bump (run `POST /api/reprobe`, diff `CapabilityFlags`, re-read
  `docs/CAPABILITY_COVERAGE.md`).

## Roadmap (owner review)

- **CI hardening:** D5 (done), D11 (done), D6 (done — strict `--max-warnings 0`).
- **Run-model unification:** D1, D2, D3, D4 (all done 2026-05-30).
- **Chat-runtime maintainability:** D10 (chat store safety-netted 2026-05-30; hook/panel coverage remains), then D7 (god-component splits — done 2026-05-30; `SettingsPanel` + `Composer` + `FilesSideTree` all < 500 LOC).
- **Capability evolution:** D8, D9, and the D12 capability roadmap.
- **UI/UX correctness & polish (owner review round 2, 2026-05-31):**
  D21 (chat/ rename — **done 2026-05-31**) → D17 (Composer profile re-scope —
  **done 2026-05-31**) → D18 (chat workspace/cwd awareness — **done 2026-05-31**) → D19
  (sidebar module navigation — **done 2026-05-31**) → D20 (UI-conventions contract — **done 2026-05-31**) → D22 (PageTopBar
  header-height design). D14 (post-D7 god-components) folds in where
  `SessionRecents`/`ModelsPanel` get touched.
